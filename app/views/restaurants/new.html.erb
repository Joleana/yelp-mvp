<h1>Add new restaurant 🚀</h1>
<%= simple_form_for(@restaurant) do |r| %>
  <%= r.label :name %>
  <%= r.text_field :name %>

  <%= r.label :address %>
  <%= r.text_field :address %>

  <%= r.label :category %>
  <%= r.text_field :category %>

  <%= r.submit %>
<% end %>
